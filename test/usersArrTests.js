var chai = require('chai');
var should = chai.should();
var usersArr = require('../models/usersArr.js');

describe('UsersArray', function(){
  //Test addUser adds correctly
  it('', function(done){


    done();
  });

  //Test getUser when user does/doesn't exist

  //Test isUsernameUnique when username is/isn't there (or arr empty)


});
